# 记忆模板 —— 一条记忆 = 一个文件 = 一个事实

<!--
写法公式：根因 + 坑 + 杠杆
- 根因：判读公式或结论本身
- 坑：这次浪费了时间的岔路，下次要绕开的
- 杠杆：下次遇到同类问题的最快入口
索引行（放进 MEMORY 索引）必须把价值压进一行 hook。
不要记「修了什么」（git 都有），要记「下次最快怎么搞定」。
-->

---
name: short-kebab-case-slug
description: 一行 hook —— 根因/坑/杠杆压缩进一句话，让未来的会话几秒内判断这条有没有用
---

**根因**：<判读公式或结论。例：命中率 = 缓存读 / (输入 + 缓存读)，误算成 缓存读/输入 会虚高>

**坑**：<这次走过的岔路。例：X 服务日志时区是 CEST 不是 UTC，直接对时间线会差两小时>

**杠杆**：<下次的最快入口。例：先查 Y 表 Z 字段，能在 5 分钟内分流三种根因>

---

## 事故排查类记忆：直接写成 playbook

**症状**：<用户/监控看到什么。例：后台探活面板全红>

**判读**：<怎么区分根因。例：看错误签名——签名 A = 池子限流塌方；签名 B = 网关误伤探活回环；长期红的 2 个是僵尸监控，别混判>

**动作**：<每种根因对应的处置。例：签名 A → 扩容池子；签名 B → 加豁免规则，改配置即生效不用重启>
